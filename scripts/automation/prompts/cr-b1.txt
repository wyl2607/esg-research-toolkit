你是该仓库的维护 agent。请完成 CR-B1：从 UploadPage.tsx 抽出 AutoFetch Panel 到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（section 抽取通用规则与验证门槛，本 CR 全部适用）。

本 CR specifics:
- 目标文件: `frontend/src/pages/UploadPage.tsx`（约 984 行）。
- 目标 Panel 锚点: `t('upload.autoFetchTitle')` —— 这是该文件里第一个 `<Panel>`，起始约 line 384；结束为它的 closing `</Panel>`，约 line 866。
- 新组件路径: `frontend/src/components/upload/AutoFetchPanel.tsx`
- 组件名: `AutoFetchPanel`
- **同时搬移 2 个 page-top helper**（只给这个 panel 用）:
  - `sourceHintLabelKey`（约 line 40）
  - `latestAutoFetchEvidence`（约 line 54）
  - 这两个 helper 一起搬到新组件文件顶部（或者 `frontend/src/pages/upload/utils.ts`，自行取舍，目标是 UploadPage.tsx 彻底看不到它们）。
- 这个 Panel 里包含 auto-fetch 表单 + pending 队列渲染 + lane telemetry 展示，会用到大量 state / props / callback，**全部当 props 传入新组件**，不要在组件里重新 useQuery / useState。
- 如果 Panel 内部使用了只在主组件 scope 里定义的 memoized 对象（例如 `lanePriorityOrder`, `sourceHints` 等），也作为 props 传入。
- 本 CR 不改另外两个 Panel（`upload.batchProgress` 和 Success Panel），不改 UploadPage 顶层 state / query 声明。

硬约束补充（覆盖 SOP 通用规则之上）:
- UploadPage.tsx 里 callback 函数（如 handleFetch / handleApprove / handleReject 等）保持在主组件定义，以 props 传入新组件。
- **不改 backend API、不改 lib/api.ts、不改 types.ts**。

提交: `refactor(frontend): extract AutoFetchPanel from UploadPage (CR-B1)`

完成后输出:
  CR-B1_RESULT: section=AutoFetchPanel new_file=<path> page_loc_delta=<before>-><after> lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
