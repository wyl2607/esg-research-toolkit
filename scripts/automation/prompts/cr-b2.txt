你是该仓库的维护 agent。请完成 CR-B2：从 UploadPage.tsx 抽出 Batch Progress Panel 到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（section 抽取通用规则与验证门槛，本 CR 全部适用）。

本 CR specifics:
- 目标文件: `frontend/src/pages/UploadPage.tsx`（约 984 行）。
- 目标 Panel 锚点: `t('upload.batchProgress')` —— 这是该文件里第二个 `<Panel>`，起始约 line 867；结束为它的 closing `</Panel>`，约 line 951。
- 新组件路径: `frontend/src/components/upload/BatchProgressPanel.tsx`
- 组件名: `BatchProgressPanel`
- 依赖数据: 该 Panel 用 `batchStatus` + 相关数据，把实际用到的变量作为 props 传入。
- 不要动第一个 Panel（AutoFetch, `t('upload.autoFetchTitle')`，由 CR-B1 处理）和第三个 Panel（Success, line ~952 起，由 CR-B3 处理）。
- 本 CR 只改第二个 Panel 的 jsx 块 + 1-2 行 import，不碰主组件 state / callback 声明。

硬约束: 与 SOP 一致；不改 backend、不改 types.ts、不改其它 panel。

提交: `refactor(frontend): extract BatchProgressPanel from UploadPage (CR-B2)`

完成后输出:
  CR-B2_RESULT: section=BatchProgressPanel new_file=<path> page_loc_delta=<before>-><after> lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
