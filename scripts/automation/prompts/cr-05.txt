你是该仓库的维护 agent。请完成 CR-05：consistency_check.sh 守卫脚本。

背景:
- docs/policies/project-consistency-rules.md §1 列出了若干 SSoT 规则，目前无自动 gate。
- 需要一个 grep-based 脚本，在 pre-push / CI 里跑，任何违规立即 fail。
- 见 §7 CR-05。

任务:
1. 创建 `scripts/consistency_check.sh`（chmod +x），bash 严格模式 `set -euo pipefail`。
2. 实现下列检查，任一失败即 exit 1 并打印具体文件:行号:违规片段:
   a. framework_version 字面量重复：在 esg_frameworks/*.py（除 schemas.py）中搜索
      `framework_version\s*=\s*["'][^"']+["']` —— 出现即违规。
   b. 任何 python 文件里出现 FRAMEWORK_VERSIONS 的重复字面量拷贝（grep `"eu_taxonomy"\s*:\s*"2020/852"` 等模式，除 schemas.py 外均违规）。
   c. frontend/src/pages/*.tsx 未出现 `useTranslation` 且含非 ASCII 可见字符（`[^\x00-\x7F]`）—— 违规（i18n 缺口）。
   d. report_parser/ 下存在未被 main.py import 的 router 模块（实现思路：grep `APIRouter(` 找到定义 router 的文件，再 grep main.py 是否 include，缺失即违规）。
   e. i18n 三语 JSON key 集合必须一致：frontend/src/i18n/locales/{en,de,zh}.json 顶层 key 集合必须相等（用 python -c 比较，差集非空即违规）。
3. 脚本顶部 usage 注释：单独跑 `bash scripts/consistency_check.sh`，也可在 scripts/review_push_guard.sh 里 source。
4. 所有违规集中收集后一次性报告（不要第一条就退出）；退出码 = 违规条数，上限 255。
5. 输出格式：`[consistency] RULE=<a-e> FILE=<path> LINE=<n> DETAIL=<...>`，最后一行 `[consistency] total_violations=<n>`。
6. 在仓库现状下跑一次脚本，当前已知违规应暴露（CR-01/CR-02/CR-03 尚未修复时）：至少规则 a、d 触发。把一次运行输出截图式地贴进 commit message 末尾。
7. 提交：`feat(scripts): add consistency_check.sh SSoT guard (CR-05)`。

硬约束:
- 纯 bash + 标准工具（grep / python3 / jq 可选）；不要引入新 npm / pip 依赖。
- 脚本必须 shellcheck 友好（`bash -n` 通过；如有 shellcheck 更好）。
- 不触碰 CR-01/02/03 的实际修复代码——本任务只写守卫，不修违规。
- 不跳 hooks、不 push。

完成后输出:
  CR-05_RESULT: script=scripts/consistency_check.sh violations_on_current_head=<n> commit=<sha>
