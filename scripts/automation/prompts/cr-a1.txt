你是该仓库的维护 agent。请完成 CR-A1：从 CompanyProfilePage.tsx 抽出 Identity Panel 到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（本 CR 的通用规则与验证门槛，全部适用）。

本 CR specifics:
- 目标 Panel 锚点: `t('profile.identityTitle')`（在 CompanyProfilePage.tsx 内搜这个 key 定位 Panel 起始；Panel 结束就是它的 closing `</Panel>` —— 下一个顶级 `<Panel>` 出现之前）。
- 新组件路径: `frontend/src/components/company-profile/IdentityCard.tsx`
- 组件名: `IdentityCard`
- 依赖数据: 这个 Panel 主要用 `identitySummary` 和 `profile.company_name`。把这两个（加必要的 t 调用）作为 props 传入。
- 类型来源: `@/lib/types` 里找 identitySummary 对应的 type（grep `identitySummary` 或 `CompanyIdentitySummary`）。

提交: `refactor(frontend): extract IdentityCard from CompanyProfilePage (CR-A1)`
