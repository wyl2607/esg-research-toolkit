你是该仓库的维护 agent。请完成 CR-A2：从 CompanyProfilePage.tsx 抽出 Data Quality Panel 到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（本 CR 的通用规则与验证门槛，全部适用）。

本 CR specifics:
- 目标 Panel 锚点: `t('profile.dataQualityTitle')`。Panel 起始为该 title 所在的 `<Panel` 标签；结束为它的 closing `</Panel>`。**不要越界**包含后面独立的 Panel（例如 framework detail Panel @ `profile.detailTitle` 不在本 CR 范围）。
- 新组件路径: `frontend/src/components/company-profile/DataQualityCard.tsx`
- 组件名: `DataQualityCard`
- 依赖数据: 观察该 Panel 内实际使用的变量（类似 `dataQuality`, `profile`, metrics 等），**原样**作为 props 传入，不要在组件里重新计算 useMemo。
- 如果 Panel 内部引用了 `deltaToneClass` / `asPct` 等 helper，从 `@/pages/company-profile/utils` import。

提交: `refactor(frontend): extract DataQualityCard from CompanyProfilePage (CR-A2)`
