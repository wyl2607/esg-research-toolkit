你是该仓库的维护 agent。请完成 CR-B3：从 UploadPage.tsx 抽出 Upload Success Panel 到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（section 抽取通用规则与验证门槛，本 CR 全部适用）。

本 CR specifics:
- 目标文件: `frontend/src/pages/UploadPage.tsx`（约 984 行）。
- 目标 Panel: 文件里第三个 `<Panel>`（无 title，渲染上传成功信息）。起始约 line 952；grep `t('upload.success')` 可精确定位。结束为它的 closing `</Panel>`。
- 新组件路径: `frontend/src/components/upload/UploadSuccessPanel.tsx`
- 组件名: `UploadSuccessPanel`
- 依赖数据: `result` 对象（至少含 `company_name`, `report_year` 等字段），加必要的 t 调用。把 result 作为 prop 传入。该 Panel 一般只在 `result` 存在时渲染（即 `{result ? <Panel>...</Panel> : null}`），保持相同的条件渲染逻辑。
- 不要动第一个 Panel（AutoFetch, CR-B1）和第二个 Panel（BatchProgress, CR-B2）。
- 本 CR 是 Wave B 里最小的一块，聚焦在这一小段 jsx 的抽离。

硬约束: 与 SOP 一致；不改 backend、不改 types.ts、不改其它 panel。

提交: `refactor(frontend): extract UploadSuccessPanel from UploadPage (CR-B3)`

完成后输出:
  CR-B3_RESULT: section=UploadSuccessPanel new_file=<path> page_loc_delta=<before>-><after> lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
