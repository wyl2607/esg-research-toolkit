你是该仓库的维护 agent。请完成 CR-A0：把 CompanyProfilePage.tsx 顶部的纯工具函数抽到独立 utils 模块，为后续 section 组件化做准备。

背景:
- 文件: `frontend/src/pages/CompanyProfilePage.tsx`（1382 行，其中 246 行开始才是 React 主组件）。
- 文件顶部 37-223 行集中了 19 个纯函数 helper 和 1 个小型内部组件 `DeferredHeavyCharts`（line 225-244）。后续 CR-A1..A6 会把 8 个 Panel 各自抽成独立 section 组件，这些组件都会 import 同一批 helper，所以必须先把它们搬到共享位置。
- 这些 helper 全部是纯函数（无 React hook、无外部副作用），搬出去风险极低。

目标:
1. 新建 `frontend/src/pages/company-profile/utils.ts`，把以下 19 个函数**按原顺序、原实现**整段搬过去，全部 `export`：
   - `asPct`
   - `asNum`
   - `deltaNumber`
   - `deltaPercent`
   - `deltaPctLabel`
   - `deltaPercentLabel`
   - `deltaToneClass`
   - `metricDisclosureLabel`
   - `metricLabelsFromKeys`
   - `asDate`
   - `compactList`
   - `sourceOriginLabel`
   - `mergeEvidenceAnchor`
   - `evidenceRichness`
   - `normalizeProfileEvidenceAnchor`
   - `parseCompanyReportId`
   - `buildCompanyTrendData`
   - `prettifyToken`
   - `frameworkRunKey`
2. 必要 import（types / 常量）一起搬过去；`utils.ts` 不应 import 任何 React hook 或 page-level 模块，只 import `@/lib/types` 和必要的类型。
3. `DeferredHeavyCharts`（line ~225-244）**保持在 CompanyProfilePage.tsx**，因为它用了 React `<Suspense>` + `lazy`，不是纯函数。
4. `CompanyProfilePage.tsx` 顶部 import 改为 `from './company-profile/utils'`（相对路径），删除原 def，保证所有 call site 继续指向同名函数。
5. 如果某些 helper 之间互相调用（例如 `normalizeProfileEvidenceAnchor` 调用 `mergeEvidenceAnchor`），搬过去后在 utils 内部自然可见，不需要跨模块 import。

硬约束:
- **不改任何函数实现、不改签名、不改行为**。
- 不抽 `DeferredHeavyCharts`、不抽 `CompanyProfilePage` 主组件本身（那是后续 CR 的事）。
- 不改其它页面、不改测试。
- 不跳 hooks、不 push、不 --amend。
- 如果发现某个 helper 闭包捕获了主组件 scope 里的变量（理论上不应该，因为它们都在主组件外定义），abort 并报告。

验证（必须全部通过）:
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm run test:smoke`（只 smoke，不跑完整 playwright）

提交信息: `refactor(frontend): extract CompanyProfilePage helpers into company-profile/utils (CR-A0)`

完成后输出:
  CR-A0_RESULT: helpers_moved=<n> new_file=frontend/src/pages/company-profile/utils.ts page_loc_delta=<before>-><after> lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
