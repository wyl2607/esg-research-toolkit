你是该仓库的维护 agent。请完成 CR-A5：从 CompanyProfilePage.tsx 抽出 Period/History Panel 区域到独立组件。

先读 `scripts/automation/prompts/cr-a-section-sop.txt`（本 CR 的通用规则与验证门槛，全部适用）。

本 CR specifics:
- 目标区域锚点: `t('profile.periodTitle')`。
- 抽取范围: 当前包含 `profile.periodTitle` 的 `<Panel>` 以及其所在的外层 `<div className="grid gap-4 lg:grid-cols-2"> ... </div>`（右侧是 `EvidenceCard` 调用）。
- 新组件路径: `frontend/src/components/company-profile/PeriodHistoryCard.tsx`
- 组件名: `PeriodHistoryCard`
- 依赖数据: `profile.periods` 及该区域内实际用到的其他变量/函数（如 `prettifyToken`、`t`）。
- 约束: 保留右侧 `EvidenceCard` 渲染行为；组件只做展示，不搬迁 useMemo 计算。

边界约束:
- 不要改动 `t('profile.detailTitle')` 对应 Panel。
- 不要改动 `t('profile.narrativeTitle')` 对应 Panel。
- 不要改动测试文件。

提交: `refactor(frontend): extract PeriodHistoryCard from CompanyProfilePage (CR-A5)`

验证:
- `cd frontend && npm run lint`
- `cd frontend && npm run build`
- `cd frontend && npx tsc --noEmit`
- `cd frontend && npm run test:smoke`（如果是仓库既有无关失败，可保留 commit 但要明确记录失败原因）

完成后输出:
CR-A5_RESULT: section=PeriodHistoryCard new_file=frontend/src/components/company-profile/PeriodHistoryCard.tsx page_loc_delta=<before>-><after> lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
