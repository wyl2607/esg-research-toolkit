你是该仓库的维护 agent。请完成 CR-06：report_parser/api.py 里 metric 名单的 SSoT 化。

背景:
- 文件: report_parser/api.py（~1393 行）
- 现状：下列 4 处硬编码了高度重叠的 metric 名单：
  - 约 62–75 行 `_PROFILE_METRICS`（11–13 项）
  - 约 225–237 行 `_manual_evidence_summary` 内 `metric_keys`
  - 约 285–299 行 `_upload_evidence_summary` 内 `fallback_metrics`
  - 约 459–471 行 `_KEY_DISCLOSURE_METRICS`
- 目标：只留一个 canonical source，其它派生。

任务:
1. 在文件顶部附近（或新建 report_parser/_metric_catalog.py）定义 canonical 列表，例如：
     CORE_METRICS = (...)  # 全量 canonical 名单
     PROFILE_METRICS = CORE_METRICS  # 或显式子集 tuple
     DISCLOSURE_KEY_METRICS = (... 子集或全集 ...)
   优先在 api.py 内定义常量（简单；除非需要跨文件再共享再抽包）。
2. 把上述 4 处替换为对该常量的引用；行为严格保持一致（顺序 + 成员集合都要一致）。
3. 绝对不要改打分/evidence/endpoint 的语义，不要改字段名。
4. pytest: `OPENAI_API_KEY=dummy .venv/bin/pytest -q tests/test_report_parser.py`（2400+ 行测试，必须全绿）。
5. 全套回归：`OPENAI_API_KEY=dummy .venv/bin/pytest -q`。
6. 一致性守卫：`bash scripts/consistency_check.sh` 必须 `total_violations=0`。
7. 提交：`refactor(report_parser): consolidate metric lists to single source-of-truth (CR-06)`。

硬约束:
- 不跳 hooks、不 push、不 --amend。
- 任何测试失败立刻 revert 并报告根因假设。
- 不触碰 frontend、不改 schema。

完成后输出:
  CR-06_RESULT: sources_collapsed=<n> commit=<sha> full_suite=<passed/failed/count>
