你是该仓库的维护 agent。请完成 CR-07：将 report_parser/api.py 里的 evidence helpers 抽到独立子模块，减小 api.py 体量并形成清晰边界。

背景:
- 文件: report_parser/api.py（当前 ~1356 行；目标是把 evidence 相关的纯函数搬走，让 api.py 聚焦在 FastAPI route + 业务编排）。
- 已在 CR-06 引入了 CORE_METRICS / DISCLOSURE_KEY_METRICS / PROFILE_METRICS / UPLOAD_FALLBACK_METRICS 这四个 SSoT 常量，`_manual_evidence_summary` 和 `_upload_evidence_summary` 会用到。
- 目标：新建 `report_parser/_evidence.py`，把以下函数整体搬过去（保持签名、docstring、内部实现完全不变）：
    - `_parse_evidence_summary`
    - `_normalize_evidence_anchor`
    - `_evidence_anchors_for_record`
    - `_manual_evidence_summary`
    - `_upload_evidence_summary`
    - `_metric_snippet`
    - `_metric_anchor_from_record`
    - `_structured_metric_evidence`
- 这些函数当前都是下划线开头的 module-private helper，没有被 api.py 外部 import（需要先 grep 验证），所以搬迁是安全的。

任务:
1. 先 grep 验证没有跨模块引用：
     grep -rn "_parse_evidence_summary\|_normalize_evidence_anchor\|_evidence_anchors_for_record\|_manual_evidence_summary\|_upload_evidence_summary\|_metric_snippet\|_metric_anchor_from_record\|_structured_metric_evidence" --include='*.py' .
   预期只命中 report_parser/api.py 自己（以及它们的定义）。如果发现任何外部 import / 测试直接 patch，**立即 abort 并报告**（本 CR 不做跨模块重构）。
2. 创建 `report_parser/_evidence.py`：
   - 顶部放必要 import（typing, json, 其它被这些函数使用到的符号）。
   - 注意 `_manual_evidence_summary` 和 `_upload_evidence_summary` 引用 `DISCLOSURE_KEY_METRICS` / `UPLOAD_FALLBACK_METRICS`，这两个常量留在 api.py 作为 public SSoT，**用 from .api import ... 会造成循环 import**。解决办法二选一（优先 A）：
     - A) 把 `CORE_METRICS` / 派生常量一起搬到 `_evidence.py`，然后 api.py 改成 `from ._evidence import CORE_METRICS, DISCLOSURE_KEY_METRICS, PROFILE_METRICS, UPLOAD_FALLBACK_METRICS`。
     - B) 把这些常量搬到一个更小的 `report_parser/_metric_catalog.py`，`_evidence.py` 和 `api.py` 都从那里 import。
   - 搬完后 `_evidence.py` 不应该 import `report_parser.api`。
3. 在 api.py 顶部：`from ._evidence import (_parse_evidence_summary, _normalize_evidence_anchor, ...)`，删除原 def。保留下划线名称不变，避免 call site 改动。
4. 如果发现这些 helper 里互相调用或调用了 api.py 其它 helper（例如 `_source_doc_id`），一并评估：
   - 如果是小依赖（几行），搬过去或在 _evidence.py 内复制最小版本（加注释说明）。
   - 如果是大依赖（涉及 SQLAlchemy models、Session 等），**abort 并报告**，本 CR 不做复杂解耦。
5. 验证（必须全部通过，任何一项失败立刻 revert 整个 CR）：
   - `OPENAI_API_KEY=dummy .venv/bin/pytest -q tests/test_report_parser.py`
   - `OPENAI_API_KEY=dummy .venv/bin/pytest -q`（全套 172+ 测试）
   - `bash scripts/consistency_check.sh`（total_violations=0）
   - `python -c "from report_parser import api; print(len(dir(api)))"` 能正常 import，不循环。
6. 提交：`refactor(report_parser): extract evidence helpers into _evidence module (CR-07)`。

硬约束:
- 不改任何函数签名、不改行为、不改 endpoint。
- 不跳 hooks、不 push、不 --amend。
- 不触碰 frontend。
- 任何 circular import / 测试失败立刻 revert 并报告根因假设，**不硬凑**。

完成后输出:
  CR-07_RESULT: helpers_moved=<n> new_module=report_parser/_evidence.py api_py_loc_delta=<before>->
<after> commit=<sha> full_suite=<passed/failed/count>
