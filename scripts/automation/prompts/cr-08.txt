你是该仓库的维护 agent。请完成 CR-08：删除 BackendOfflineBanner 组件，改为 NoticeBanner 调用。

背景:
- `frontend/src/components/BackendOfflineBanner.tsx`（~15 行）是对 `NoticeBanner.tsx` 的硬编码 tone=info 薄包装。
- NoticeBanner 已支持多种 tone（info/warning/error/success）和自定义 title/body + icon 映射。
- 删 wrapper 省一个组件文件 + 3-4 个 import 入口。

任务:
1. 先 grep 所有对 `BackendOfflineBanner` 的引用：
     grep -rn "BackendOfflineBanner" frontend/src --include='*.tsx' --include='*.ts'
   预期 3-4 处。
2. 每个引用位置：
   - 用 `<NoticeBanner tone="info" title={t('...')}>...</NoticeBanner>` 替换
   - 用原 BackendOfflineBanner 内部的 i18n key / 文案（不要新造 key）
   - 如原组件还传了额外 action / dismiss 回调，按 NoticeBanner 的 props 映射过去；若 NoticeBanner 缺该 prop，就**保持等价功能**（不为此重构 NoticeBanner，超出本 CR 范围）
3. `git rm frontend/src/components/BackendOfflineBanner.tsx`
4. 验证:
   - `cd frontend && npm run lint`
   - `cd frontend && npm run build`
   - `cd frontend && npm run test:smoke`（只跑 smoke spec，不要跑完整 playwright）
5. `grep -r "BackendOfflineBanner" frontend/src` 应返回 0 行。
6. 提交：`refactor(frontend): inline BackendOfflineBanner into NoticeBanner (CR-08)`。

硬约束:
- 不改 NoticeBanner 的 API，不添加新 prop。
- 不改页面布局/路由/行为。
- 不跳 hooks、不 push。
- 如果 NoticeBanner 某个特性缺失导致等价替换困难，**abort 并报告**，不要硬凑。

完成后输出:
  CR-08_RESULT: refs_updated=<n> file_deleted=BackendOfflineBanner.tsx lint=<pass/fail> build=<pass/fail> smoke=<pass/fail> commit=<sha>
