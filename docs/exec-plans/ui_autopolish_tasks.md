

## Autopolish run 2026-04-16T23:36:53



## Autopolish run 2026-04-17T00:03:36


### CRITICAL

- [ ] **[/ · desktop]** (Top emitters chart data formatting) The x-axis uses long raw numbers without abbreviated units, making the scale hard to read at a glance. → Format axis ticks and tooltip values into compact units like `25M`, `50M`, `100M` using a formatter such as `Intl.NumberFormat('de-DE', { notation: 'compact', maximumFractionDigits: 1 })` and add an axis label for emissions unit. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Sample size / data reliability) The table shows N=1 for every metric, which makes the percentile output potentially misleading without a strong inline warning near the results. → Add a prominent warning banner directly above the table when sample size is below threshold using `rounded-xl border border-amber-300 bg-amber-50 px-4 py-3 text-sm text-amber-900` and include logic like `if (n < 5) showWarning` instead of relying only on the distant helper note. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Percentile table) The benchmark table is too dense for mobile, with cramped columns and wrapped metric labels that severely hurt readability. → Replace the desktop table on mobile with stacked metric cards or a horizontally scrollable container using `overflow-x-auto -mx-5 px-5`; if keeping the table, set `min-w-[560px] text-sm whitespace-nowrap` and pin the metric column. _(effort: l)_

### HIGH

- [ ] **[/ · desktop]** (Global language consistency) The page mixes German, English, and Chinese in navigation, chart labels, and controls, which makes the product feel unfinished and harms comprehension. → Normalize all visible UI copy through i18n keys and locale-specific content; ensure one active locale drives labels for nav, charts, legends, and controls, e.g. replace hardcoded strings with `t('dashboard.uploadTrend')`, `t('coverage.defaultView')`, and localized dataset labels. _(effort: m)_
- [ ] **[/ · desktop]** (Header content block) The page title, eyebrow, and description do not establish a strong enough hierarchy relative to the large amount of white space and surrounding controls. → Tighten the hero stack with `space-y-3`, set eyebrow to `text-xs font-semibold tracking-[0.18em] text-neutral-500`, title to `text-5xl font-semibold leading-tight`, and description to `max-w-3xl text-base leading-7 text-neutral-600` to improve scannability. _(effort: s)_
- [ ] **[/ · desktop]** (KPI cards) The KPI cards show raw numbers without enough semantic framing, trend context, or differentiated visual hierarchy between label and value. → Refactor KPI cards to use `p-6`, label `text-sm font-medium text-neutral-600`, value `mt-3 text-5xl font-semibold tracking-tight`, and optional delta row `mt-2 text-sm`; if no delta exists, add subtitle text like 'Aktive Abdeckung im Portfolio'. _(effort: m)_
- [ ] **[/ · desktop]** (Chart titles) The chart titles are oversized and heavy relative to chart content, causing the headings to dominate the data. → Reduce panel titles to `text-2xl font-semibold leading-tight` or `text-xl font-semibold` and add `mb-4` instead of large type as the main emphasis. _(effort: xs)_
- [ ] **[/ · desktop]** (Upload trend chart) The x-axis includes a non-sequential year jump to 2026 without explanation, which makes the chart appear unreliable or incomplete. → Validate and sort the dataset before render; if future-year data is intentional, add a note or visually distinguish forecast values with a different fill like `fill-amber-300` plus legend label 'Prognose'. _(effort: m)_
- [ ] **[/ · desktop]** (Top emitters chart labels) Company labels are truncated or duplicated awkwardly, making the chart difficult to trust and scan. → Increase left label gutter width and apply label formatting with controlled wrapping or truncation plus tooltip; in Recharts set a wider `YAxis width={140}` and render custom ticks with `text-sm text-neutral-700`. _(effort: s)_
- [ ] **[/ · desktop]** (Chart semantics) The 'Top 5 Emittenten (Scope 1)' chart lacks visible unit labeling, so users cannot tell whether values represent tCO2e, kg, or another measure. → Add a subtitle or axis label like 'tCO2e' beneath the chart title or next to the x-axis using `text-sm text-neutral-500`, and include it in tooltip headers. _(effort: xs)_
- [ ] **[/ · desktop]** (Data coverage legend) The coverage legend is dense, multilingual, and too small for comfortable desktop reading. → Increase legend text to `text-sm`, use consistent locale labels, add `gap-4 flex-wrap`, and separate the progress summary from legend items with `mt-3 pt-3 border-t border-neutral-100`. _(effort: s)_
- [ ] **[/ · desktop]** (Dashboard storytelling) The page shows metrics and charts but does not clearly explain the user workflow or why these insights matter for ESG regulatory intelligence. → Add a compact insight strip or status summary below the header, e.g. three badges for 'Neue Uploads', 'Abdeckungsrisiken', 'Höchste Emittenten', each linking to the next action; implement as a `grid grid-cols-3 gap-4` of clickable summary cards. _(effort: m)_
- [ ] **[/upload · desktop]** (Upload dropzone) The primary action lacks enough visual prominence, so the main upload task does not dominate the page despite being the page's core purpose. → Increase emphasis by giving the dropzone a tinted background and stronger border, e.g. `className='mt-8 rounded-3xl border-2 border-amber-300 bg-amber-50/40 hover:bg-amber-50 transition-colors'`, and add a clear CTA button inside such as `Button className='mt-4 bg-amber-600 hover:bg-amber-700 text-white'` with label 'Datei auswählen'. _(effort: s)_
- [ ] **[/upload · desktop]** (Hero content) The explanatory note on the right is visually detached from the title and body copy, creating a split focal point and weakening scanning order. → Convert the header into a two-column grid with tighter alignment and constrain the note width, e.g. `className='grid grid-cols-[minmax(0,1fr)_360px] gap-8 items-start'`, then style the note as a secondary callout with `p-5 rounded-2xl border border-amber-200 bg-amber-50 text-amber-900` and reduce top offset so it aligns with the title block. _(effort: m)_
- [ ] **[/companies · desktop]** (Hero + filter toolbar) The page’s primary action is unclear because search, sorting, and export actions all have similar visual weight and are spread across a large low-density container. → Make search the dominant control by increasing its width to `w-[360px]`, giving it stronger emphasis with `bg-white border-slate-300 shadow-sm`, and demoting export buttons to secondary ghost buttons using `border-slate-200 text-slate-600 hover:bg-slate-50`; group sort actions in a secondary row with `flex gap-3 flex-wrap` and move exports to the far right using `ml-auto`. _(effort: m)_
- [ ] **[/companies · desktop]** (Header metrics) The KPI cards compete with the page title and description, weakening the top-down scanning order. → Reduce KPI card prominence by shrinking padding from roughly `p-6` to `p-4`, using number sizes like `text-3xl font-semibold` and labels `text-[11px] tracking-[0.18em] text-slate-500`, while increasing title prominence to `text-5xl font-semibold` and constraining description width with `max-w-2xl`. _(effort: s)_
- [ ] **[/companies · desktop]** (Company cards) Each card lacks a clear primary action because the company name, icon, and descriptive text all look passive and there is no obvious click target treatment. → Turn the entire card into an interactive surface using `group cursor-pointer hover:shadow-md hover:border-slate-300 transition`, make the company name a link styled with `group-hover:text-amber-700`, and add a small CTA like `Profil öffnen →` in `text-sm font-medium text-amber-700 mt-2`. _(effort: m)_
- [ ] **[/companies · desktop]** (Card grid density) The cards are too tall for the amount of information shown, reducing scan efficiency and limiting how many companies can be compared at once on desktop. → Compress card height by reducing outer padding to `p-5`, shrinking the description to two lines with `line-clamp-2`, reducing metric tile padding to `p-3`, and decreasing vertical gaps between sections to `gap-4`. _(effort: m)_
- [ ] **[/taxonomy · desktop]** (Main content layout) The desktop canvas is mostly empty, making the product feel unfinished and reducing perceived value at first glance. → Constrain the main content to a more intentional max width and add a two-step layout with stronger content grouping, e.g. `className='max-w-6xl px-10 py-10'` on the page container and place the selector plus helper content in a `grid grid-cols-12 gap-8` with the primary flow spanning `col-span-8`. _(effort: m)_
- [ ] **[/taxonomy · desktop]** (Primary action / selector) The company-year dropdown does not read as the clear primary action even though the whole page depends on it. → Increase prominence with larger height, stronger border, and explicit CTA styling such as `h-14 text-base border-slate-300 hover:border-slate-400 focus:ring-2 focus:ring-amber-500 focus:border-amber-500 bg-white shadow-sm`, and add an adjacent primary button like `Analysieren` if selection is not auto-triggered. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Primary action area) The main CTA is visually weak because the dropdown looks like a passive input rather than the primary action that unlocks the page. → Increase prominence of the selection block by wrapping the label + select in a `bg-white border border-slate-200 shadow-sm rounded-2xl p-6` container and add a secondary CTA button like `Weiter` or `Vergleich starten` with `bg-indigo-600 hover:bg-indigo-700 text-white font-medium px-4 py-2 rounded-lg` aligned to the right of the select on desktop. _(effort: m)_
- [ ] **[/frameworks · desktop]** (Main content canvas) The page leaves most of the 1440px viewport empty, which makes the product feel unfinished and reduces information scent. → Constrain content to a clearer grid such as `max-w-6xl` and introduce a 2-column desktop layout (`grid grid-cols-12 gap-6`) with the form in `col-span-7` and a preview/help panel in `col-span-5` showing available frameworks, expected output, or sample metrics. _(effort: l)_
- [ ] **[/compare · desktop]** (Main workflow area) The page has no visible primary action or next-step affordance after selecting companies, so the scanning order stops at the chip list. → Add a sticky or inline primary CTA such as a disabled/enabled button below or above the chips, e.g. `<Button className="mt-6 self-start bg-indigo-600 hover:bg-indigo-700 text-white rounded-xl px-5 py-2.5 disabled:bg-slate-200 disabled:text-slate-500">Vergleich starten</Button>` and enable it once at least 2 companies are selected. _(effort: s)_
- [ ] **[/compare · desktop]** (Empty state) The large blank area with a single pale sentence communicates very little about what the comparison view will show or why the feature is useful. → Replace the placeholder text with a structured empty state containing an icon, short headline, 1-2 benefit bullets, and a preview skeleton of comparison columns using `border border-slate-200 rounded-2xl bg-white/70 p-8 text-center` plus `grid grid-cols-3 gap-4 mt-6` skeleton cards. _(effort: m)_
- [ ] **[/benchmarks · desktop]** (Header / page intro) The page does not communicate a strong primary workflow because the title, helper note, filters, and data card all use similar visual weight and spacing. → Create a stronger hierarchy by tightening the intro stack to `space-y-2`, reducing the helper note prominence with `bg-stone-50 text-stone-600 border-stone-200`, and increasing the filter card prominence using `bg-white shadow-sm border border-stone-200` while keeping the results card slightly lighter with `shadow-xs`. _(effort: m)_
- [ ] **[/benchmarks · desktop]** (Filter action button) The primary action 'Benchmarks neu berechnen' looks like a neutral secondary button, so it does not clearly stand out as the next step. → Restyle the action as a true primary CTA with classes like `bg-amber-600 hover:bg-amber-700 text-white border-transparent shadow-sm font-medium px-5 h-12 rounded-xl focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-500`. _(effort: xs)_
- [ ] **[/benchmarks · desktop]** (Benchmark table storytelling) The table presents raw percentiles but provides almost no interpretation of what the user should learn from the benchmark results. → Add a compact summary row or chip group above the table showing sample size, selected industry/year, and benchmark reliability state; e.g. `flex flex-wrap gap-2` with badges `inline-flex items-center rounded-full bg-stone-100 px-3 py-1 text-sm text-stone-700`, and surface a warning badge when `n < 5`. _(effort: m)_
- [ ] **[/ · mobile]** (Top navigation) The language switcher occupies too much vertical space on mobile and pushes the dashboard content below the fold. → Collapse language options into a single globe/language menu button on mobile using `md:hidden` for the full selector row and replace with a compact trigger like `h-10 px-3 rounded-xl border border-neutral-200 bg-white`. _(effort: m)_
- [ ] **[/ · mobile]** (KPI semantics) Using blue for one KPI and green for the others implies different semantic meanings without explanation, which can confuse users. → Reserve semantic green/red for positive/negative status only; otherwise use a neutral brand color for all KPI values such as `text-indigo-600` and add trend chips for semantic states if needed. _(effort: s)_
- [ ] **[/ · mobile]** (Trend chart) The chart appears cramped on mobile, with large title text consuming space and limited room left for axis labels and bars. → Reduce chart card title to `text-2xl leading-tight font-semibold`, decrease card padding to `p-4`, and set a fixed chart area such as `h-56` with responsive margins for labels. _(effort: m)_
- [ ] **[/ · mobile]** (Chart readability) Axis labels and gridlines are too subtle and small relative to the card width, which hurts legibility at 390px. → Use `text-xs fill-slate-500` for chart ticks, darken gridlines to a token like `stroke-stone-300`, and reduce the number of ticks shown on mobile to avoid clutter. _(effort: m)_
- [ ] **[/upload · mobile]** (Top language/header controls) The language switcher occupies too much vertical and visual space on mobile, competing with the page title and pushing the primary task lower on the screen. → Reduce control height and padding from roughly `h-12 px-5` to `h-10 px-3`, shrink label text to `text-sm`, and move the language options into a horizontal compact segmented control with `gap-2 overflow-x-auto whitespace-nowrap` or a single language menu button. _(effort: m)_
- [ ] **[/upload · mobile]** (Primary action / upload zone) The upload dropzone does not read as the clear primary action because it uses a low-contrast dashed border, muted icon, and no explicit CTA button styling. → Strengthen the dropzone with `border-2 border-orange-300 bg-white`, increase icon size to `w-10 h-10`, make the main label `text-lg font-semibold text-slate-800`, and add a visible CTA such as a button inside: `inline-flex h-11 items-center rounded-lg bg-indigo-600 px-4 text-white hover:bg-indigo-700` labeled 'Datei auswählen'. _(effort: m)_
- [ ] **[/companies · mobile]** (Primary task flow) The page lacks a clear primary action or next step, so search, sorting, and export controls compete equally for attention. → Promote search as the primary action by placing it first with stronger visual emphasis (`className='h-12 text-base border-slate-300 shadow-sm'`) and demote export actions into a secondary section or collapsible menu using a `details`/popover pattern; add a visible companies list or preview directly beneath search. _(effort: m)_
- [ ] **[/companies · mobile]** (Control panel) All filter and export buttons are stacked as full-width-height pills with equal visual weight, creating a long, inefficient vertical block on mobile. → Convert actions into a tighter vertical rhythm using `gap-3` with button heights reduced to `h-10 px-4 text-sm`, and group sort chips in a flex-wrap row (`flex flex-wrap gap-2`) while moving export buttons into a separate subsection with a top border (`pt-4 mt-4 border-t border-slate-200`). _(effort: s)_
- [ ] **[/companies · mobile]** (Data density / content completeness) At this viewport, the screen shows only summary and controls but no visible company records, so users cannot immediately verify what is stored or act on actual data. → Render at least the first few company rows/cards directly below the controls on initial load, using a compact mobile list component (`divide-y divide-slate-200`) with company name, source tags, year, and key metric chips; if empty, show a purposeful empty state with CTA. _(effort: l)_
- [ ] **[/taxonomy · mobile]** (Primary action / form section) The main task on the page is unclear because the company-year selector is visually understated and competes with the long introductory copy. → Promote the selector to the primary action by moving it directly under the headline/description, increasing visual emphasis with `bg-white`, `border-slate-300`, `shadow-sm`, `min-h-[56px]`, and adding a CTA-style label above it such as `text-sm font-semibold text-slate-900` instead of the muted section label. _(effort: m)_
- [ ] **[/taxonomy · mobile]** (Storytelling) The page explains the disclosure mirror concept but does not quickly demonstrate what the product outputs or why the user should engage. → Add a compact preview module below the selector or in the empty state showing sample outputs like 'Ausrichtung', 'Lücken', and 'DNSH-Signale' as 3 chips/cards using `grid grid-cols-1 gap-3` on mobile, so the page communicates the product's analytical value before data is loaded. _(effort: l)_
- [ ] **[/frameworks · mobile]** (Hero / above-the-fold) The primary action is too visually weak relative to the large title block, so the user’s next step is not immediately obvious on first scan. → Turn the company/year selector into a stronger primary CTA by increasing contrast and prominence: apply `bg-white border border-slate-300 shadow-sm focus:ring-2 focus:ring-violet-500`, add a visible label-action grouping, and consider adding a filled button below on mobile such as `className='mt-3 h-11 w-full rounded-xl bg-violet-600 text-white font-medium'` with text like 'Analyse starten'. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Title and intro copy) The title block is visually strong but too tall for mobile, pushing the task-oriented content too far down the viewport. → Tighten the hero stack on mobile using `text-4xl leading-tight` or `text-[42px] leading-[1.05]`, reduce bottom margin to `mb-6`, and constrain the subtitle to `text-base leading-7 text-slate-600 max-w-[32ch]`. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Data density / content completeness) Most of the lower viewport is empty, making the page feel unfinished and lowering confidence in the workflow. → Use the below-the-fold area to preview what the comparison will include with 2–3 compact cards or skeleton states (`grid grid-cols-1 gap-4 mt-10`) such as 'Regulatory overlap', 'Disclosure requirements', and 'Timeline differences'. _(effort: m)_
- [ ] **[/compare · mobile]** (Hero header) The headline and descriptive copy dominate the viewport, pushing the actual selection task below the fold and weakening the primary action on mobile. → Reduce top-section vertical space by changing the hero wrapper from large desktop-like spacing to `pt-4 pb-6` on mobile, set the heading to `text-4xl leading-tight` instead of oversized display styling, and limit the description to `text-base leading-7 max-w-[32ch]`. _(effort: s)_
- [ ] **[/compare · mobile]** (Selection card) The main compare module lacks a clear action path because there is no visible selected-state summary or primary CTA near the company list. → Add a sticky bottom action bar or footer inside the card with a disabled/enabled primary button like `className='sticky bottom-0 mt-4 flex items-center justify-between border-t bg-white/95 px-4 py-3 backdrop-blur'` and button states `bg-indigo-600 text-white disabled:bg-slate-200 disabled:text-slate-400`. _(effort: m)_
- [ ] **[/compare · mobile]** (Company chips) All company pills look visually identical, so users cannot tell selected, hovered, focused, or disabled states at a glance. → Implement explicit state styles: default `border-slate-300 bg-white text-slate-800`, selected `border-indigo-600 bg-indigo-50 text-indigo-700 ring-2 ring-indigo-200`, focus-visible `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500`, and pressed `active:scale-[0.98]`. _(effort: s)_
- [ ] **[/compare · mobile]** (Long company names) Very long labels create oversized pills that dominate rows and make the list harder to scan or compare visually. → Constrain chip content with `max-w-[calc(100vw-6rem)] truncate whitespace-nowrap` and expose full text via tooltip/sheet on tap, or move to a 2-column selectable list component on mobile instead of free-width pills. _(effort: m)_
- [ ] **[/benchmarks · mobile]** (Select field - Branche) The selected branch label is long and risks feeling cramped inside the input at this viewport width. → Apply `truncate pr-8` to the value label container and ensure the select trigger uses `flex items-center justify-between gap-2` so long NACE labels do not collide with the chevron. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Primary CTA) The recalculation button lacks visual prominence and currently resembles a disabled or secondary action. → Switch to a solid primary treatment such as `bg-violet-600 text-white hover:bg-violet-700 active:bg-violet-800 shadow-sm` with `font-semibold h-11 rounded-xl`, and reserve neutral outlined styles for secondary actions. _(effort: xs)_
- [ ] **[/benchmarks · mobile]** (Table typography) The table header and values are too small and tightly packed, making percentile comparison difficult at a glance. → Increase table text to `text-sm`, use `leading-5`, add `px-3 py-2.5` cell padding, and set headers to `font-semibold text-neutral-600 uppercase tracking-wide text-[11px]` only if enough horizontal space is provided. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Metric naming in table) Metric names wrap mid-word and break the scan pattern, which makes the table feel noisy and untrustworthy. → Give the first column a fixed width such as `w-40 min-w-40`, apply `break-words` only as fallback, and prefer human-readable labels with units separated into muted subtext or a second line. _(effort: m)_

### MEDIUM

- [ ] **[/ · desktop]** (Primary CTA) The upload button is visually prominent but feels detached from the page heading and lacks supporting context for what uploading enables. → Align the CTA with the title block in a shared `flex items-start justify-between` header row and add a secondary helper line or tooltip such as 'PDF, XLSX, CSV' using `text-sm text-neutral-500`; also add `hover:bg-amber-700 focus-visible:ring-2 focus-visible:ring-amber-500`. _(effort: s)_
- [ ] **[/ · desktop]** (Sidebar navigation) Sidebar items have inconsistent emphasis and the active state relies too heavily on a pale yellow fill with limited contrast. → Increase active-item contrast with `bg-amber-100 text-amber-900 border border-amber-200` and make inactive items `text-neutral-700 hover:bg-neutral-50`; standardize row height with `h-11 px-3 rounded-xl gap-3`. _(effort: s)_
- [ ] **[/ · desktop]** (Sidebar section rhythm) The spacing between section headers and nav items is inconsistent, making the sidebar scan less cleanly than it should. → Use a fixed section pattern like `mb-6`, `px-6`, section labels with `mb-3 text-[11px] font-semibold tracking-[0.22em] text-neutral-500`, and item list wrapper `space-y-1.5`. _(effort: xs)_
- [ ] **[/ · desktop]** (KPI card color semantics) The KPI values use different colors without clear semantic meaning, which can imply status differences where none are explained. → Reserve green exclusively for positive performance/status and use a single neutral or brand color for standard metrics, e.g. `text-violet-600` or `text-neutral-900`; only apply `text-emerald-600` when accompanied by an explicit positive delta chip. _(effort: s)_
- [ ] **[/ · desktop]** (Card container styling) Cards rely on very soft borders and shadows, which reduces separation between modules on the warm off-white background. → Use a stronger card token such as `bg-white border border-neutral-200 shadow-sm rounded-3xl` and keep a consistent elevation level across KPI cards and chart panels. _(effort: xs)_
- [ ] **[/ · desktop]** (Upload trend chart readability) The chart gridlines, axes, and bars are too close in visual weight, reducing quick data reading. → Set gridlines to `stroke-neutral-200`, axes to `stroke-neutral-400`, and bars to a single brand color with hover state; add top padding and tick font `text-xs fill-neutral-500` for cleaner contrast. _(effort: s)_
- [ ] **[/ · desktop]** (Data coverage section header) The control chip in the top-right of the coverage card is too visually isolated and its purpose is unclear. → Replace the floating pill with a labeled segmented control or dropdown aligned in the card header row, e.g. `flex items-center gap-2`, trigger `h-9 px-3 text-sm border border-neutral-200 rounded-full bg-white` with a localized label. _(effort: s)_
- [ ] **[/ · desktop]** (Typography system) The page mixes a serif display title with sans-serif body and large serif chart titles without a clear type scale system. → Limit serif usage to the page title only, and standardize content typography with a scale such as `text-xs`, `text-sm`, `text-base`, `text-xl`, `text-2xl`; apply sans-serif titles to cards and charts for consistency. _(effort: m)_
- [ ] **[/ · desktop]** (Page spacing rhythm) Vertical spacing between the header, KPI row, charts, and lower card is serviceable but not systematic, making the dashboard feel slightly uneven. → Adopt a consistent page rhythm using `space-y-8` for major sections, `gap-6` for grid layouts, and card internals with `p-6` or `p-8` rather than mixed spacing values. _(effort: s)_
- [ ] **[/ · desktop]** (Accessibility contrast) Several secondary text elements and pale fills appear close to minimum contrast, especially in the sidebar headers and inactive metadata. → Use accessible neutral tokens such as `text-neutral-600` or darker for metadata and ensure pale backgrounds like yellow active states still provide 4.5:1 contrast for text; verify with Tailwind tokens around `neutral-700` on `amber-100`. _(effort: s)_
- [ ] **[/upload · desktop]** (Page body spacing) There is too much unused vertical space below the dropzone, which makes the page feel incomplete and reduces perceived product depth. → Reduce page min-height or add supporting content below the uploader, such as accepted file types, processing steps, or recent uploads; if keeping this layout, tighten the canvas with `pb-16` instead of oversized empty space and set the main container to `max-w-6xl` with `space-y-8`. _(effort: m)_
- [ ] **[/upload · desktop]** (Typography hierarchy) The text styles between eyebrow, title, description, field label, and helper text are too similar in tone, making the hierarchy flatter than it should be. → Strengthen the type ramp: eyebrow `text-xs tracking-[0.2em] font-semibold text-slate-500 uppercase`, title `text-5xl leading-tight font-semibold text-slate-900`, body `text-lg leading-8 text-slate-600`, field label `text-sm font-medium text-slate-800`, helper `text-sm text-slate-500`. _(effort: s)_
- [ ] **[/upload · desktop]** (Select field) The industry select looks visually generic and slightly detached from the rest of the page's premium styling. → Refine the input container with stronger height, padding, and focus state, e.g. `h-14 rounded-2xl border border-slate-200 bg-white px-4 text-base shadow-sm focus:ring-2 focus:ring-amber-500 focus:border-amber-500`, and wrap label + helper in a `space-y-1.5` group. _(effort: s)_
- [ ] **[/upload · desktop]** (Color accessibility) Several low-emphasis texts and pale borders appear close to failing WCAG AA on the light background, especially helper text and dashed upload border. → Darken secondary text from light gray/blue to `text-slate-600` or `text-slate-500`, and increase dashed border contrast to `border-slate-300`; avoid amber text on pale amber backgrounds unless using `text-amber-900`. _(effort: xs)_
- [ ] **[/upload · desktop]** (Upload microcopy) The upload area does not communicate key operational details like supported formats, file size, or what happens after upload, reducing trust for a document-processing tool. → Add a tertiary helper line under the CTA, e.g. `text-sm text-slate-500 mt-2` with 'PDF, DOCX, XLSX • max. 50 MB pro Datei • Extraktion wird gespeichert', and consider a 3-step inline checklist below the dropzone. _(effort: s)_
- [ ] **[/upload · desktop]** (Storytelling) The page explains uploading a report but does not clearly connect that action to the product outcome of ESG disclosure analysis and reusable company cases. → Add a short benefits row under the intro or upload area with 3 bullets/cards, e.g. 'Bericht extrahieren', 'Kennzahlen strukturieren', 'Firmenfall wiederverwenden', using `grid grid-cols-3 gap-4 mt-6` and compact icon cards. _(effort: m)_
- [ ] **[/companies · desktop]** (Hero section spacing) There is too much empty space between the page intro and the functional controls, causing the content to feel disconnected and pushing the card grid lower than necessary. → Tighten vertical rhythm by reducing wrapper spacing from something like `py-12 gap-8` to `py-8 gap-6`, and reduce the filter container top margin to `mt-6`. _(effort: s)_
- [ ] **[/companies · desktop]** (Page title and intro copy) The descriptive copy is informative but too long and visually similar to body text, so it slows scanning and weakens product storytelling. → Shorten the paragraph to one concise sentence and style it as supporting text with `text-base leading-7 text-slate-600 max-w-2xl`; keep the eyebrow label smaller using `text-xs uppercase tracking-[0.22em] text-slate-500`. _(effort: xs)_
- [ ] **[/companies · desktop]** (Company card typography) The text hierarchy inside cards is muddy because the description, metadata chips, and metrics all use similar contrast and density. → Increase hierarchy by setting company names to `text-[32px] leading-9 font-semibold` equivalent for this card scale (`text-3xl` is too large; use `text-[1.75rem]` or `text-2xl`), descriptions to `text-sm leading-6 text-slate-600`, and chip labels to `text-xs font-medium text-slate-600`. _(effort: s)_
- [ ] **[/companies · desktop]** (Metric blocks in cards) The three KPI tiles inside each card are cramped and the line breaks in labels like 'Taxonomie-Umsatz' feel forced and uneven. → Use a 3-column grid with `grid grid-cols-3 gap-3` and fixed internal padding `p-4`; set metric labels to `text-[11px] uppercase tracking-[0.18em] leading-4 break-normal hyphens-auto`, and if needed provide shorter display labels such as `Taxonomie-Umsatz` on one line with `min-h-[32px]`. _(effort: m)_
- [ ] **[/companies · desktop]** (Filter buttons) All sorting controls appear as independent buttons rather than a cohesive filtering system, which makes the toolbar feel fragmented. → Convert sort actions into a segmented or grouped filter row using shared container styles like `inline-flex rounded-xl border border-slate-200 overflow-hidden` and child buttons with `px-4 py-2 border-r last:border-r-0 bg-white hover:bg-slate-50`. _(effort: m)_
- [ ] **[/companies · desktop]** (Color system) Some light gray text and border combinations appear close to failing WCAG AA, especially in metadata chips, descriptions, and tertiary labels. → Standardize muted text to at least `text-slate-600`, secondary labels to `text-slate-500`, and borders to `border-slate-200`; avoid very pale beige/gray backgrounds behind small text unless text is `text-slate-700` or darker. _(effort: s)_
- [ ] **[/companies · desktop]** (Card metadata chips) The year and source badges are useful but too visually similar to each other, making source provenance harder to parse quickly. → Differentiate semantics by giving year a neutral pill `bg-slate-100 text-slate-700` and source type a tinted pill such as `bg-blue-50 text-blue-700 border-blue-200`, both with `text-xs font-medium rounded-full px-3 py-1`. _(effort: s)_
- [ ] **[/companies · desktop]** (Storytelling / product clarity) The page shows stored company records but does not strongly explain what users can do next with these records beyond opening a profile. → Add a short subheader or helper row above the grid such as `35 Unternehmen • sortierbar nach Jahr, Taxonomie und Quelle • als JSON/CSV exportierbar` using `text-sm text-slate-600`, and include card-level affordances like 'Profil öffnen' or 'Mit Vergleich hinzufügen'. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Visual hierarchy) The section label, title, description, disclaimer, filter label, selector, and empty state all have similar visual weight, so scanning order is flatter than it should be. → Create a clearer type and spacing ladder: use `text-xs tracking-[0.18em] text-slate-500` for the eyebrow, `text-5xl font-semibold` for the H1, `text-lg text-slate-600 max-w-4xl` for the intro, and add larger vertical gaps like `space-y-8` between major blocks while tightening label-to-input spacing to `mb-2`. _(effort: m)_
- [ ] **[/taxonomy · desktop]** (Spacing rhythm) There is too much vertical separation between the selector area and the empty-state card while some internal label-to-input spacing feels slightly loose. → Standardize on an 8px spacing scale using wrappers like `space-y-6` for sections and `space-y-2` inside field groups; reduce the gap above the result card from roughly 32px to `mt-6` and tighten the field block with `mb-6`. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Content width consistency) The disclaimer box spans much wider than the selector block and card below it, creating a visually awkward zig-zag edge. → Align all primary content blocks to the same width container, for example wrap headline, body copy, notice, filter block, and empty state in `max-w-5xl` or split into a consistent `max-w-4xl` main column. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Empty state card) The empty state is too generic and does not explain what insights the user will get after selecting a company and year. → Rewrite the empty state to preview value, e.g. 'Nach Auswahl sehen Sie Ausrichtungsquoten, DNSH-Signale, Zielbereichsabdeckung und Offenlegungslücken', and add 2–3 bullet points or mini chips using `text-sm text-slate-600 flex gap-2 flex-wrap mt-3`. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Color contrast) Several secondary text elements and light borders appear close to the minimum contrast threshold on the warm off-white background. → Darken muted text and border tokens to safer values such as `text-slate-600`/`text-slate-700` and `border-slate-300`, and avoid very pale gray outlines on cream backgrounds to maintain WCAG AA. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Interactive states) There is no visible evidence of strong hover, focus, or open states for the dropdown, which hurts usability and accessibility. → Ensure trigger states include `hover:bg-slate-50`, `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-amber-500 focus-visible:ring-offset-2`, and rotate the chevron on open with a state class like `data-[state=open]:rotate-180`. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Storytelling) The page explains what the mirror is, but it does not quickly communicate how this helps ESG analysts make decisions or compare disclosures. → Add a short benefit strip below the intro with 3 compact items like 'Ausrichtungsquoten prüfen', 'DNSH-Lücken erkennen', 'Zielbereichs-Evidenz nachverfolgen' in a `grid grid-cols-3 gap-4` using icon + text cards. _(effort: m)_
- [ ] **[/taxonomy · desktop]** (Sidebar density) The sidebar uses large vertical spacing that is easy to scan but consumes a lot of space and makes the app feel less information-rich on desktop. → Tighten nav rows to around `h-10` with `gap-3 px-3 text-[15px]` and reduce section spacing using `space-y-1` so more product structure is visible without scrolling. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Page header) The eyebrow label, title, and subtitle are too close in visual weight, so scanning order is flatter than it should be. → Reduce eyebrow emphasis with `text-xs tracking-[0.18em] text-slate-500 uppercase`, keep title at `text-4xl font-semibold text-slate-900`, and set subtitle to `mt-3 text-base text-slate-600` with a max width like `max-w-3xl`. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Section spacing) Vertical rhythm is inconsistent, with the header, field label, select, and empty state stacked too tightly at the top while the rest of the page has excessive dead space. → Normalize spacing with a 24/32px rhythm: `pt-10` for page content, `mt-8` between header and form section, `space-y-3` inside form groups, and `mt-8` before the result/empty state card. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Dropdown field) The select control has low perceived affordance and the chevron/icon treatment is too subtle for a primary workflow step. → Increase input clarity with `h-12 px-4 bg-white border-slate-300 hover:border-slate-400 focus:border-indigo-500 focus:ring-4 focus:ring-indigo-100 rounded-xl text-slate-800` and make the chevron `text-slate-500` at `w-5 h-5`. _(effort: xs)_
- [ ] **[/frameworks · desktop]** (Empty state card) The empty state reads like a placeholder instead of a helpful onboarding component because it repeats the field instruction without explaining the comparison output. → Rewrite the card to describe what appears after selection and add bullet points or chips for 'EU-Taxonomie', 'CSRC 2023', and 'CSRD/ESRS'; structure it with `flex items-start gap-4 p-6 bg-white rounded-2xl border border-slate-200 shadow-sm`. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Storytelling) The screen does not clearly communicate what insights the framework comparison will generate, so new users may not understand the product's value. → Add a descriptive intro block under the subtitle with 1-2 lines such as comparison dimensions, coverage, and output format; use `text-sm text-slate-600 max-w-2xl` and optionally three small feature chips with `inline-flex rounded-full bg-slate-100 px-3 py-1 text-xs font-medium`. _(effort: s)_
- [ ] **[/compare · desktop]** (Selection card header) The helper text is too understated and does not clearly indicate current progress toward the 4-company limit. → Turn the instruction into a stronger subheader with live count, e.g. `text-sm font-medium text-slate-700` and append a badge like `<span className="ml-2 inline-flex items-center rounded-full bg-slate-100 px-2 py-0.5 text-xs font-medium text-slate-600">0/4</span>`. _(effort: s)_
- [ ] **[/compare · desktop]** (Company chips) The chips look static because there is no visible selected, hover, or focus state shown in the component styling. → Add explicit interactive states: default `bg-white border-slate-300 text-slate-800`, hover `hover:border-indigo-300 hover:bg-indigo-50`, selected `bg-indigo-600 border-indigo-600 text-white`, focus `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-indigo-500 focus-visible:ring-offset-2`. _(effort: s)_
- [ ] **[/compare · desktop]** (Content width) The main content stretches very wide on desktop, which weakens focus and makes the chip cloud feel horizontally loose. → Constrain the content column with a max width such as `max-w-6xl` or `max-w-5xl` on the main wrapper and use `px-8 xl:px-10` to keep the content centered and denser. _(effort: xs)_
- [ ] **[/compare · desktop]** (Vertical rhythm) Spacing between the title, description, selection card, and empty state is inconsistent, making the page feel sparse rather than intentionally structured. → Use a defined stack rhythm on the page container such as `space-y-8` and reduce the gap below the selection card while giving the empty state a dedicated section wrapper with `pt-10` instead of a large undifferentiated blank region. _(effort: s)_
- [ ] **[/compare · desktop]** (Typography hierarchy) The descriptive copy under the page title is readable but visually too close in prominence to instructional text inside the card. → Differentiate levels with a clearer type ramp: title `text-5xl font-semibold tracking-tight`, intro `text-lg leading-8 text-slate-600 max-w-3xl`, card label `text-sm font-medium text-slate-700`, placeholder/empty copy `text-base text-slate-500`. _(effort: s)_
- [ ] **[/compare · desktop]** (Card styling) The selection card shadow is very diffuse and soft, which reduces edge definition on a similarly light background. → Use a crisper elevation recipe like `bg-white border border-slate-200 shadow-[0_8px_24px_rgba(15,23,42,0.06)] rounded-[28px]` instead of relying on a broad ambient shadow alone. _(effort: xs)_
- [ ] **[/compare · desktop]** (Storytelling) The screen does not preview any comparison dimensions such as disclosures, KPIs, or frameworks, so the product's ESG intelligence value is not evident. → Add a compact preview strip below the selector showing 3-4 upcoming comparison categories as cards, e.g. `grid grid-cols-4 gap-4` with labels like 'Offenlegung', 'KPIs', 'Rahmenwerke', 'Peer Performance' using muted icons and short descriptors. _(effort: m)_
- [ ] **[/compare · desktop]** (Sidebar navigation) The active nav item is highlighted, but inactive items are visually similar in weight and spacing, making the menu feel heavy. → Reduce inactive emphasis with `text-slate-600 font-medium`, keep active at `text-amber-900 bg-amber-100`, and tighten nav rhythm using `gap-1` between items plus `px-3 py-2.5 rounded-xl` for each row. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Top helper banner) The explanatory note is visually bulky and reads like a disabled form field rather than contextual guidance. → Convert it into an inline info alert component with `rounded-xl border border-stone-200 bg-stone-50 px-4 py-3 text-sm leading-6 text-stone-600` and add an info icon using a flex row `flex items-start gap-3`. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Filter form layout) The form labels and controls are readable but the horizontal spacing is uneven, making the branch select feel oversized relative to year and action. → Use a 12-column grid such as `grid grid-cols-12 gap-4 items-end` with Branche `col-span-6`, Jahr `col-span-3`, and CTA `col-span-3`, and standardize control heights with `h-12`. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Select inputs) The select fields have minimal interactive affordance and the borders are too faint to clearly indicate clickability on desktop. → Increase control definition with `border-stone-300 bg-white hover:border-stone-400 focus:border-amber-500 focus:ring-2 focus:ring-amber-100 rounded-xl text-base` and ensure the chevron icon uses `text-stone-500`. _(effort: xs)_
- [ ] **[/benchmarks · desktop]** (Results card header) The timestamp competes awkwardly with the section title and is not visually separated as supporting metadata. → Render the timestamp as subdued helper text below the title using `text-sm text-stone-500 mt-1` and add more separation before the table with `mb-5` on the header block. _(effort: xs)_
- [ ] **[/benchmarks · desktop]** (Table typography) The metric labels are slightly too small and compressed for dense ESG terminology, reducing scan speed. → Increase table readability with `text-sm` body copy, `leading-6`, and use `font-medium text-stone-700` for the first column while keeping numeric columns `tabular-nums text-stone-900`. _(effort: xs)_
- [ ] **[/benchmarks · desktop]** (Table row rhythm) The table rows lack enough vertical padding and striping/division to support quick row tracking across many percentile columns. → Increase row padding to `py-3` or `py-3.5`, add subtle zebra striping with `even:bg-stone-50/50`, and keep separators `border-b border-stone-100`. _(effort: xs)_
- [ ] **[/benchmarks · desktop]** (Card styling) The shadows and borders across cards are very soft and similar, making sections blend together instead of feeling intentionally layered. → Standardize surface tokens: base cards `rounded-3xl border border-stone-200 bg-white shadow-[0_1px_2px_rgba(0,0,0,0.04)]`, elevated controls `shadow-sm`, and avoid large diffuse shadows. _(effort: m)_
- [ ] **[/benchmarks · desktop]** (Content width) The main content stretches comfortably but lacks a stronger max-width rhythm, so the title, note, filters, and table feel vertically stacked rather than compositionally organized. → Wrap content in a constrained container such as `max-w-6xl mx-auto px-8 py-8` and reduce inter-section gaps to a consistent `space-y-6`. _(effort: m)_
- [ ] **[/ · mobile]** (Header hero) The hero section has excessive vertical padding for a mobile dashboard, reducing the amount of actionable data visible without scrolling. → Tighten the hero stack spacing by reducing container padding from something like `py-10 px-4` to `py-6 px-4` and use `space-y-4` instead of larger gaps between eyebrow, heading, body text, and CTA. _(effort: s)_
- [ ] **[/ · mobile]** (Visual hierarchy) The primary action is clear, but the KPI section starts too far below it, weakening the scanning flow from hero to metrics. → Reduce the gap between the CTA and first KPI card to `mt-6` or `gap-4` and group the KPI cards into a tighter section wrapper with `space-y-4`. _(effort: s)_
- [ ] **[/ · mobile]** (KPI cards) The KPI cards are visually oversized for the amount of content they contain, creating low information density on a small viewport. → Reduce card min-height and internal padding, e.g. change to `p-5 rounded-2xl` with `min-h-[104px]` or remove fixed height altogether so labels and values sit more compactly. _(effort: s)_
- [ ] **[/ · mobile]** (KPI typography) The label-to-value type scale is inconsistent, with labels feeling relatively heavy and values lacking a standardized numeric hierarchy. → Set labels to `text-sm font-medium text-slate-600` and KPI values to `text-5xl leading-none font-semibold tabular-nums` for consistent emphasis across all cards. _(effort: s)_
- [ ] **[/ · mobile]** (CTA button) The primary button is visually strong but slightly oversized relative to the mobile layout and lacks visible pressed/focus states. → Keep the fill color but refine sizing to `h-12 px-5 text-base font-semibold rounded-2xl` and add interaction classes like `active:scale-[0.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-amber-600`. _(effort: xs)_
- [ ] **[/ · mobile]** (Body copy) The supporting paragraph is readable but slightly dense and long for mobile scanning under a large headline. → Constrain body text to `text-[15px] leading-7 text-slate-600 max-w-[32ch]` and consider inserting a manual line break or shortening copy length on mobile. _(effort: s)_
- [ ] **[/ · mobile]** (Card styling) The card borders and shadows are so subtle that cards can blend into the warm background in lower-quality displays. → Strengthen card separation with `border border-stone-200/80 shadow-sm` and keep `bg-white rounded-3xl` for clearer elevation. _(effort: xs)_
- [ ] **[/ · mobile]** (Storytelling) The page says 'Dashboard' but does not quickly explain what specific ESG intelligence tasks or outcomes users can accomplish beyond uploading reports. → Add a compact secondary summary block under the hero or above KPIs with 2-3 chips or bullets such as `ESG signals`, `taxonomy alignment`, and `renewables share`, using `flex flex-wrap gap-2 text-sm` badges. _(effort: m)_
- [ ] **[/ · mobile]** (Mobile navigation) The hamburger button and theme control have equal visual weight to the primary content, competing with the hero heading. → Reduce chrome prominence by using lighter styling like `border-neutral-200 bg-white/80 text-slate-600` and smaller button widths (`size-11`) while keeping the CTA as the strongest colored element. _(effort: s)_
- [ ] **[/upload · mobile]** (Visual hierarchy) The introductory paragraph is too long and visually close in weight to supporting text, which weakens scanning order under the main headline. → Constrain the intro to `text-base leading-7 text-slate-600 max-w-[34ch]` and split into shorter lines or two sentences; reduce nonessential copy and add `mt-3` below the headline and `mt-6` before the callout for clearer progression. _(effort: s)_
- [ ] **[/upload · mobile]** (Typography) The helper text below the select field is too small and low contrast for comfortable mobile reading. → Increase helper text to `text-sm leading-5 text-slate-600` and ensure a contrast-safe token instead of pale brown/gray; avoid going below 14px on mobile for instructional copy. _(effort: xs)_
- [ ] **[/upload · mobile]** (Spacing rhythm) Vertical spacing between sections feels inconsistent, with the callout, select field, and dropzone separated by uneven gaps that make the form feel loosely assembled. → Adopt a consistent spacing scale such as `space-y-6` for major sections and `space-y-2` within form groups; wrap the content stack in a parent `flex flex-col space-y-6` instead of per-element ad hoc margins. _(effort: s)_
- [ ] **[/upload · mobile]** (Select field) The field container and input blend together, making the form control boundaries feel soft and slightly ambiguous. → Use a clearer field pattern: outer group `space-y-2`, label `text-sm font-medium text-slate-700`, and select `h-11 rounded-xl border border-slate-300 bg-white px-4 text-base shadow-sm focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200` while removing excessive wrapper decoration. _(effort: s)_
- [ ] **[/upload · mobile]** (Accessibility / interactive states) Interactive elements do not visibly communicate pressed, hover, or keyboard focus states in the static design, which risks poor accessibility and unclear affordance. → Add consistent states: buttons and controls should include `focus:outline-none focus:ring-2 focus:ring-indigo-300 focus:ring-offset-2 active:scale-[0.99]`, and the dropzone should get `hover:border-orange-400 hover:bg-orange-50 focus-within:ring-2 focus-within:ring-orange-200`. _(effort: s)_
- [ ] **[/upload · mobile]** (Storytelling / product context) The page explains uploading an ESG report but does not quickly reinforce the value proposition of converting files into a reusable company case for regulatory intelligence. → Add a short benefit row under the intro or inside the upload card with 2–3 bullets using `text-sm text-slate-600` and small icons, e.g. 'Automatische Extraktion', 'Wiederverwendbarer Firmenfall', 'Basis für Meldungen'. _(effort: s)_
- [ ] **[/companies · mobile]** (KPI cards) The metric cards have weak hierarchy because the labels are heavily letter-spaced while the card values and layout do not strongly distinguish importance. → Reduce label tracking from wide uppercase to a more readable style (`text-[11px] tracking-[0.12em] font-medium text-slate-500`) and increase metric prominence with `text-4xl font-semibold text-slate-900 mt-3`; keep card padding consistent at `p-5`. _(effort: s)_
- [ ] **[/companies · mobile]** (KPI grid rhythm) The first row contains two half-width cards while the third KPI spans full width, but the spacing and card heights make the grid feel slightly arbitrary rather than intentionally structured. → Use a consistent mobile KPI grid with `grid grid-cols-2 gap-4` and allow the third card to span both columns via `col-span-2`, while setting all cards to the same minimum height such as `min-h-[112px]` for cleaner rhythm. _(effort: s)_
- [ ] **[/companies · mobile]** (Typography) The title, eyebrow, body text, labels, and button copy do not form a strong enough type ladder, making scanning less efficient. → Standardize the mobile type scale: eyebrow `text-[11px] tracking-[0.18em]`, title `text-4xl leading-tight font-semibold`, body `text-base leading-8 text-slate-600`, card labels `text-xs`, and button text `text-sm font-medium`. _(effort: m)_
- [ ] **[/companies · mobile]** (Search field) The search input is visually acceptable but lacks strong affordance as the core interaction and may feel too similar to the surrounding card background. → Increase contrast and focus affordance with `border-slate-300 bg-white focus:border-indigo-500 focus:ring-2 focus:ring-indigo-200`, and set the placeholder to `text-slate-400` while keeping the icon at `text-slate-400`. _(effort: xs)_
- [ ] **[/companies · mobile]** (Color system) The warm page background and very light card surfaces reduce contrast between sections, which softens information grouping on a data page. → Increase surface separation by using a stronger neutral card background and border, e.g. page `bg-stone-50`, cards `bg-white border border-slate-200`, and reserve indigo for active/primary states only. _(effort: s)_
- [ ] **[/companies · mobile]** (Storytelling) The introductory copy explains the page concept, but the screen does not visually reinforce the ESG/regulatory intelligence value through metadata like source type, taxonomy coverage, or recency. → Add secondary context near the KPIs or first records using small badges/chips such as `Regulatory source`, `Year`, `Taxonomy`, `Last updated`, styled with `inline-flex items-center rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600`. _(effort: m)_
- [ ] **[/taxonomy · mobile]** (Header) The top bar consumes too much vertical space on mobile, pushing the core content below the fold. → Reduce header height and control padding using `h-14 px-4 py-2`, shrink the language pills to `h-10 px-3 text-sm`, and collapse less-used languages into a menu or horizontal scroll container with `overflow-x-auto whitespace-nowrap`. _(effort: m)_
- [ ] **[/taxonomy · mobile]** (Visual hierarchy) The headline is very dominant while the descriptive paragraph and interactive controls sit at similar emphasis levels, creating a weak scanning path. → Tighten hierarchy with `text-4xl leading-tight font-semibold` for the headline, reduce body copy width and prominence via `text-base leading-7 text-slate-600 max-w-[34ch]`, and increase spacing between intro copy and form to a deliberate `mt-8` so sections feel intentional. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Spacing & rhythm) Vertical spacing between sections is inconsistent, making the page feel loosely stacked rather than structured. → Adopt a consistent mobile spacing scale such as `space-y-6` for page sections and standardize internal card padding to `p-5` or `p-6`; specifically reduce the gap between the selector label and input to `mt-2` and align the lower empty-state card with the same section rhythm. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Dropdown input) The select field looks disabled because the border, icon, and placeholder all use similarly light values. → Increase affordance with `border-slate-300 bg-white text-slate-700 placeholder:text-slate-500`, darken the chevron to `text-slate-400`, and add interactive states like `focus:ring-2 focus:ring-violet-500 focus:border-violet-500`. _(effort: xs)_
- [ ] **[/taxonomy · mobile]** (Empty state card) The empty state is too generic and does not help the user understand what data will appear after selection. → Rewrite the content to preview value, e.g. 'Wählen Sie ein Unternehmen und Jahr, um Offenlegungen, Lücken und DNSH-Signale zu analysieren,' and add a subtle secondary action or hint row using `text-sm text-slate-600 mt-2`; if keeping the icon card, use `items-start gap-3 p-5` for clearer structure. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Viewport data density) The mobile screen shows mostly intro content and very little analytical UI, which underutilizes the viewport and delays value discovery. → Shorten the disclaimer card with a collapsible pattern using `line-clamp-3` + 'Mehr anzeigen', or place it behind an info disclosure component so the selector and expected results move higher on the first screen. _(effort: m)_
- [ ] **[/frameworks · mobile]** (Header) The top navigation is horizontally crowded on a 390px viewport, making the language controls compete with the menu and reducing breathing room. → Reduce header density by collapsing secondary languages into a single language switcher menu on mobile; use `hidden sm:flex` for the full language list and replace with a compact dropdown trigger `flex sm:hidden`. Also tighten spacing with `gap-2 px-4 py-2` and cap button heights at `h-10`. _(effort: m)_
- [ ] **[/frameworks · mobile]** (Header buttons) The selected language pill is visually dominant while the other controls look secondary, creating imbalance in the top row. → Normalize control hierarchy by reducing the selected state weight from a large filled block to a subtler selected chip, e.g. `bg-violet-100 text-violet-700 border border-violet-200`, and give all header controls consistent sizing with `rounded-xl h-11 px-4`. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Section label) The all-caps eyebrow labels have excessive letter spacing on mobile, which hurts readability and makes the text feel detached from the content below. → Reduce tracking and size for eyebrow text on mobile: replace with `text-[11px] tracking-[0.18em] font-medium text-slate-500` instead of wider spacing, and add `mb-2` to better bind the label to the field. _(effort: xs)_
- [ ] **[/frameworks · mobile]** (Storytelling / product clarity) The page does not clearly explain what the comparison produces or why selecting a company and year matters, so the product value is still abstract. → Add a short benefit-oriented explainer under the subtitle or above the selector, e.g. a 2-line caption with `text-sm leading-6 text-slate-600` saying what users will compare (taxonomy, disclosures, gap analysis, overlap). _(effort: s)_
- [ ] **[/frameworks · mobile]** (Form field) The dropdown looks like a passive input rather than an interactive primary selector because the border and icon contrast are too subtle. → Increase affordance with `border-slate-300 bg-white shadow-sm hover:border-slate-400`, darken placeholder to `text-slate-600`, and set the chevron to `text-slate-500`. Add focus styles `focus:outline-none focus:ring-2 focus:ring-violet-500 focus:border-violet-500`. _(effort: xs)_
- [ ] **[/frameworks · mobile]** (Empty-state card) The card repeats the selector prompt without adding enough guidance, so it consumes space without moving the user closer to completion. → Convert the card into a more useful empty state with a concise title and helper bullets or examples; e.g. add `text-sm text-slate-600` guidance and a secondary action like `Beliebte Beispiele` as a link button. Keep layout compact with `p-5 flex gap-3 items-start`. _(effort: m)_
- [ ] **[/frameworks · mobile]** (Typography) The body copy in the card is readable but line length and line-height make it feel a bit loose relative to the compact mobile layout. → Set supporting copy to `text-[15px] leading-6 text-slate-700` and title to `text-base font-semibold leading-6`, limiting width with `max-w-[28ch]` if needed to improve scan consistency. _(effort: xs)_
- [ ] **[/frameworks · mobile]** (Color system / accessibility) Several neutral text and border tones are very light against the warm off-white background, risking low contrast and weak component separation. → Use stronger neutral tokens for mobile UI chrome: `text-slate-700` for secondary text, `text-slate-500` for labels, and `border-slate-300` for inputs/cards. Ensure placeholder text stays at least around `text-slate-500` rather than lighter grays. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Interactive states) The current screen does not show enough visible hover/pressed/focus affordances for key controls, which weakens perceived polish and accessibility. → Add consistent interactive state tokens across buttons and inputs: `transition-colors duration-150`, `hover:bg-slate-50`, `active:scale-[0.99]`, `focus-visible:ring-2 focus-visible:ring-violet-500 focus-visible:ring-offset-2`. _(effort: s)_
- [ ] **[/compare · mobile]** (Section label) The small uppercase eyebrow has weak visual payoff relative to the large heading and does not help scanning enough to justify the extra line. → Either remove the eyebrow on mobile or tighten it with `text-[11px] tracking-[0.18em] text-slate-500 mb-2` so it reads as metadata rather than competing content. _(effort: xs)_
- [ ] **[/compare · mobile]** (Body copy) The supporting paragraph is too long for mobile scanning and creates a dense text block before interaction begins. → Shorten the copy or constrain readability using `max-w-[30ch] text-[17px] leading-7 text-slate-600`, and reduce paragraph bottom margin to `mb-6`. _(effort: s)_
- [ ] **[/compare · mobile]** (Chip layout rhythm) The pill grid has inconsistent visual rhythm because variable chip widths create ragged rows with uneven negative space. → Use a flex-wrap container with consistent gaps like `flex flex-wrap gap-3` and standardize chip height to `min-h-11 px-4 py-2 rounded-2xl`; consider capping chip width with `max-w-full` and ellipsis behavior for long names. _(effort: s)_
- [ ] **[/compare · mobile]** (Selection card header) The instruction text inside the card is visually understated and not clearly tied to a limit or progress state. → Turn it into a stronger utility header with count feedback, e.g. `flex items-center justify-between text-sm font-medium text-slate-700` and show `0/4 ausgewählt` in a badge using `rounded-full bg-slate-100 px-2.5 py-1 text-xs`. _(effort: s)_
- [ ] **[/compare · mobile]** (Card container) The large rounded card has subtle boundaries that are elegant but too faint on mobile, reducing separation from the page background. → Increase contrast slightly with `border border-slate-200 bg-white shadow-sm rounded-[28px]` instead of relying mostly on tonal separation. _(effort: xs)_
- [ ] **[/compare · mobile]** (Typography ladder) The jump from tiny eyebrow to very large headline to medium body creates a steep type scale that feels less balanced on a narrow viewport. → Use a tighter mobile ladder such as eyebrow `text-xs`, headline `text-[42px] leading-[1.02]`, body `text-base leading-7`, section title `text-lg font-medium`, and chip text `text-[15px] font-medium`. _(effort: s)_
- [ ] **[/compare · mobile]** (Interactive affordance) The selectable chips read close to static tags because the borders, shadows, and state cues are too subtle. → Add clearer affordance with `transition-colors transition-shadow hover:border-slate-400 hover:shadow-sm active:bg-slate-50 cursor-pointer` and a check icon for selected chips using `inline-flex items-center gap-2`. _(effort: s)_
- [ ] **[/compare · mobile]** (Storytelling) The page explains company comparison conceptually but does not show what users gain after selection, such as disclosure, KPI, or risk differences. → Add a compact preview block below the selector on mobile with 2-3 mini comparison dimensions using icons and labels like `Offenlegung`, `KPIs`, `Risiken`, styled as `grid grid-cols-3 gap-2 text-center text-xs`. _(effort: m)_
- [ ] **[/benchmarks · mobile]** (Top navigation) The header controls consume too much vertical space on mobile and push the actual benchmark content below the fold. → Reduce header padding and control height from roughly `py-2.5 px-4 h-12` to `py-2 px-3 h-10`, and tighten the nav gap using `gap-2` instead of `gap-3/4` on the mobile breakpoint. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Page heading) The H1 is visually dominant to the point that it overwhelms the subtitle and compresses the rest of the mobile layout. → Scale the heading down on mobile to `text-4xl leading-tight` or `text-3xl leading-snug` and keep the subtitle at `text-base text-neutral-600 mt-2` for a cleaner hierarchy. _(effort: xs)_
- [ ] **[/benchmarks · mobile]** (Informational note) The explanatory note uses a large card with long line lengths and low visual emphasis, so it reads as a heavy block rather than helpful supporting guidance. → Decrease padding to `p-4`, set copy to `text-sm leading-6 text-neutral-700`, and add a subtle semantic accent such as `border-l-4 border-amber-300 bg-amber-50/60` to signal advisory content. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Filter card) The filter card has generous padding and field heights that create unnecessary vertical density for a simple two-field form. → Reduce card padding to `p-5`, use `space-y-5`, and set inputs to `h-11 px-4 text-base` instead of taller defaults to improve compactness without hurting tap targets. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Form labels) Field labels are visually too close in weight and size to body text, reducing scanability of the form. → Use a stronger label style such as `text-sm font-semibold text-neutral-800 mb-2` and keep helper/body copy at `font-normal text-neutral-600` for better separation. _(effort: xs)_
- [ ] **[/benchmarks · mobile]** (Card surfaces) The cards rely on very light borders and minimal elevation, causing sections to blend into the warm background. → Increase separation with `border border-neutral-200 bg-white shadow-[0_1px_2px_rgba(0,0,0,0.04)]` and use consistent radius like `rounded-3xl` across informational and results cards. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Results section heading) The results title wraps awkwardly across multiple lines, making the key insight harder to parse quickly. → Reduce the heading size on mobile to `text-2xl leading-snug`, move the sector name to a second line with smaller emphasis using `block text-xl`, or shorten the label pattern to `Perzentil-Übersicht` plus a subheading. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Table row separation) The current row dividers are subtle, so individual metrics do not separate clearly in the dense result area. → Use stronger row borders like `divide-y divide-neutral-200` and add zebra striping on mobile with `odd:bg-white even:bg-neutral-50/60` to improve trackability. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Storytelling / product clarity) The page explains percentile benchmarks but does not quickly convey why this matters for ESG regulatory workflows or how users should interpret the output. → Add a concise intro or helper sentence under the results title such as `Vergleichen Sie Ihre Offenlegung mit Unternehmen derselben NACE-Klasse` using `text-sm text-neutral-600`, or add a small legend row above the table. _(effort: s)_

### LOW

- [ ] **[/ · desktop]** (Top utility bar) The language switcher and theme toggle have inconsistent visual weight and alignment relative to each other. → Normalize controls to `h-11 px-4 rounded-xl border border-neutral-200 bg-white text-sm font-medium` with `gap-2`, and reduce the active language fill saturation slightly for better polish. _(effort: xs)_
- [ ] **[/upload · desktop]** (Sidebar active item) The active navigation state relies heavily on background tint and does not have enough contrast or structural distinction from inactive items. → Add a left accent bar and stronger text/icon color for the active item, e.g. `relative bg-amber-50 text-amber-900 before:absolute before:left-0 before:top-2 before:bottom-2 before:w-1 before:rounded-r-full before:bg-amber-600`. _(effort: s)_
- [ ] **[/upload · desktop]** (Top-right controls) Theme and language controls feel visually disconnected from the main content and draw disproportionate attention relative to their importance. → Reduce prominence by lowering saturation and grouping them in a subtle utility bar, e.g. use `text-sm text-slate-600`, `border-slate-200`, and keep only the active language as filled while others remain ghost buttons. _(effort: s)_
- [ ] **[/upload · desktop]** (Component polish) The page mixes very soft surfaces with hard dashed borders, creating a slightly unfinished component language. → Standardize radius and elevation across cards/inputs/dropzone using a shared token set such as `rounded-2xl`, `border-slate-200`, and `shadow-sm`; for the dropzone use `border-dashed` plus a subtle inner tint rather than a raw gray outline. _(effort: m)_
- [ ] **[/companies · desktop]** (Search input) The search field looks slightly underpowered relative to its importance and the placeholder contrast is faint. → Increase input height to `h-11`, add `pl-10 pr-4`, use placeholder color `placeholder:text-slate-400`, and strengthen focus with `focus:ring-2 focus:ring-amber-500/30 focus:border-amber-500`. _(effort: xs)_
- [ ] **[/companies · desktop]** (Sidebar active state) The active navigation item relies heavily on background tint and could be more distinct for accessibility and scan speed. → Add a stronger active indicator such as `border-l-4 border-amber-600 bg-amber-50 text-amber-900 font-medium pl-4` while preserving icon color alignment. _(effort: xs)_
- [ ] **[/companies · desktop]** (Top-right language/theme controls) The utility controls are visually detached from the rest of the page system and slightly overemphasized relative to page actions. → Reduce their prominence with smaller height `h-10`, use subtler borders `border-slate-200`, and align spacing with `gap-3`; keep only the active language filled while others are text-only buttons. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Typography) The uppercase labels use wide tracking and small size, which is elegant but slightly weak for fast enterprise scanning. → Keep uppercase styling but improve readability with `text-[11px] font-medium tracking-[0.14em] text-slate-500` instead of overly spaced letters. _(effort: xs)_
- [ ] **[/taxonomy · desktop]** (Component polish) The cards and form controls rely on very subtle borders and shadows, which makes them feel slightly washed out rather than intentional. → Use a more defined component recipe like `rounded-2xl border border-slate-200 bg-white shadow-[0_1px_2px_rgba(15,23,42,0.04)]` and add hover/focus transitions with `transition-colors transition-shadow`. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Top-right global controls) The theme and language controls visually compete with the page content despite being secondary utilities. → Reduce emphasis by using quieter text/buttons such as `text-sm text-slate-600`, lighter fills, and slightly smaller control height like `h-10`, while keeping the selected language state accessible. _(effort: s)_
- [ ] **[/taxonomy · desktop]** (Header separation) The top bar and main content have minimal separation, so the utilities and page content blend together slightly. → Add a subtle header bottom border like `border-b border-slate-200/80` or increase top padding in the page body to `pt-10` for clearer layering. _(effort: xs)_
- [ ] **[/frameworks · desktop]** (Sidebar active item) The active navigation state relies heavily on a pale yellow fill that may not maintain strong contrast and does not create a crisp edge against the white sidebar. → Use a more semantic active treatment such as `bg-amber-50 text-amber-900 border border-amber-200` or add a left accent `before:w-1 before:bg-amber-500` while keeping icon and label at `font-medium`. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Top-right controls) Theme and language controls compete visually with each other and with the page content because one item is filled blue while the rest are plain text links. → Group them into a compact utility bar with consistent button styling: `flex items-center gap-2`, use `text-sm text-slate-600`, and apply a lighter selected state like `bg-indigo-50 text-indigo-700 border border-indigo-200 rounded-lg px-3 py-2`. _(effort: s)_
- [ ] **[/frameworks · desktop]** (Typography contrast) Some small uppercase labels use wide tracking and light contrast, which can reduce readability for non-decorative metadata. → Tighten tracking slightly and increase contrast: replace very spaced labels with `tracking-[0.14em] text-slate-600 text-xs font-medium` and avoid using uppercase for longer instructional labels if possible. _(effort: xs)_
- [ ] **[/frameworks · desktop]** (Card polish) The empty state card border and shadow are so subtle that the component blends into the background on a bright display. → Strengthen elevation with `border-slate-200 shadow-[0_1px_2px_rgba(16,24,40,0.06),0_1px_3px_rgba(16,24,40,0.1)]` and keep the card background pure white to create separation. _(effort: xs)_
- [ ] **[/compare · desktop]** (Color contrast) The empty-state text appears too light against the warm background and may miss comfortable readability even if technically passing. → Darken informational text to `text-slate-500` or `text-slate-600` and avoid pale blue-gray for non-disabled content; reserve low-contrast text only for tertiary metadata. _(effort: xs)_
- [ ] **[/compare · desktop]** (Chip density) Long company names create uneven row lengths and visual ragging, making the chip cloud feel slightly unbalanced. → Normalize chip sizing with `max-w-full truncate` and consider a two-line constrained chip variant using `max-w-[240px] whitespace-nowrap overflow-hidden text-ellipsis` or move to a searchable multiselect list for long labels. _(effort: m)_
- [ ] **[/compare · desktop]** (Top utility nav) Language controls compete visually with each other and the selected language relies mostly on fill color without a stronger state cue. → Standardize as segmented buttons with one selection pattern: selected `bg-indigo-600 text-white`, unselected `text-slate-600 hover:text-slate-900`, and wrap in `inline-flex rounded-xl border border-slate-200 p-1 gap-1`. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Percentile emphasis) Only the p50 values are bolded, but the column header itself is not visually distinguished enough to anchor the user's eye. → Apply a subtle background tint to the full p50 column using conditional cell classes like `bg-amber-50/60` and strengthen the header with `text-stone-900 font-semibold`. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Sidebar active state) The active navigation item relies heavily on a pale yellow fill and could be more explicit for accessibility and faster scanning. → Add a left accent bar and stronger text/icon contrast using `relative bg-amber-50 text-amber-900 before:absolute before:left-0 before:top-2 before:bottom-2 before:w-1 before:rounded-r before:bg-amber-600`. _(effort: s)_
- [ ] **[/benchmarks · desktop]** (Language/theme controls) The top-right utility controls feel disconnected from the page content and create slight visual noise due to inconsistent button treatments. → Normalize them into a compact utility group with `flex items-center gap-2`, secondary ghost buttons `text-sm text-stone-600 hover:text-stone-900`, and one shared container using `rounded-2xl border border-stone-200 bg-white/80 px-2 py-1`. _(effort: s)_
- [ ] **[/ · mobile]** (Eyebrow label) The uppercase eyebrow text has weak contrast and wide tracking, making it harder to read on mobile. → Increase contrast to `text-stone-600` and reduce tracking from very wide to `tracking-[0.18em]`, keeping `text-xs font-semibold uppercase`. _(effort: xs)_
- [ ] **[/upload · mobile]** (Color system) The beige page background, yellow callout border, and brown body text create a premium aesthetic but reduce semantic clarity and may lead to inconsistent contrast across components. → Reserve warm tones for accent/information states only and move body copy to neutral tokens like `text-slate-700`; use semantic palettes such as `bg-amber-50 border-amber-300 text-amber-900` for the notice and `bg-white` for form surfaces. _(effort: m)_
- [ ] **[/upload · mobile]** (Component polish) The dashed border on the upload card appears visually weak and slightly unfinished compared with the more refined rounded controls above it. → Increase border definition with `border-[2px] border-dashed border-slate-300 rounded-3xl` and add a subtle shadow like `shadow-[0_8px_24px_rgba(15,23,42,0.06)]` to match the rest of the UI language. _(effort: xs)_
- [ ] **[/upload · mobile]** (Mobile touch ergonomics) The top-left menu button and language chips appear tappable but visually crowded, increasing the chance of accidental taps near the screen edge. → Ensure all top controls are at least `min-h-[44px]`, add `px-4`, and increase spacing between them with `gap-3`; consider moving the menu into a simpler header row separate from language selection. _(effort: s)_
- [ ] **[/companies · mobile]** (Header) The top navigation row is visually busy for mobile because the menu button, brand/control chip, and three language options compete within a tight horizontal space. → Collapse language options into a single locale selector on mobile (`hidden sm:flex` for full language list; mobile uses one button with chevron), and reduce header padding to `px-4 py-2` so more vertical space is available for content. _(effort: m)_
- [ ] **[/companies · mobile]** (Component polish) Cards and controls rely on similar border/shadow treatments, so interactive and non-interactive elements are not clearly differentiated. → Use flatter styling for static KPI cards (`shadow-sm`) and stronger interactive states for buttons/inputs (`hover:bg-slate-50 active:bg-slate-100 focus-visible:ring-2 focus-visible:ring-indigo-500`); keep radius consistent with `rounded-2xl` for cards and `rounded-xl` for controls. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Section label) The uppercase 'UNTERNEHMEN & JAHR' label has weak readability due to small size, wide tracking, and low contrast. → Use `text-xs font-medium tracking-[0.12em] text-slate-600` or switch to sentence case with `text-sm font-semibold text-slate-700` for better mobile legibility. _(effort: xs)_
- [ ] **[/taxonomy · mobile]** (Typography) Body copy line-height is comfortable but the contrast between headline, body, and labels could be stronger to improve scanning on small screens. → Define a clearer mobile type ladder: headline `text-4xl`, intro body `text-base leading-7`, card body `text-sm leading-6`, labels `text-xs/ text-sm font-medium`, and use `text-slate-900`, `text-slate-700`, `text-slate-500` consistently as semantic text tokens. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Color system) Several muted text and border treatments are close in value, reducing accessibility and making components feel washed out. → Raise contrast to meet WCAG AA by standardizing neutral tokens such as `text-slate-700` for body, `text-slate-500` for secondary text, `border-slate-200/300` for containers, and avoid ultra-light gray text on white backgrounds. _(effort: s)_
- [ ] **[/taxonomy · mobile]** (Component polish) Cards and controls use similar border radius and stroke weight but lack a clear system for elevation and interaction. → Standardize surfaces with `rounded-2xl border border-slate-200 bg-white shadow-[0_1px_2px_rgba(15,23,42,0.04)]`, and buttons/inputs with `hover:border-slate-400 active:scale-[0.99] transition` so interactive elements feel more responsive than static cards. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Spacing rhythm) The vertical spacing between the subtitle, field label, selector, and empty-state card is slightly inconsistent, weakening scan rhythm. → Use a consistent mobile spacing scale such as `mt-8` from hero to form section, `mb-2` label-to-input, and `mt-8` input-to-card. Standardize outer page gutters to `px-4` and section stacks with `space-y-6`. _(effort: s)_
- [ ] **[/frameworks · mobile]** (Card icon) The document icon container feels slightly oversized and visually detached from the text block on mobile. → Reduce icon wrapper to `h-10 w-10`, align to top with `items-start`, and use `mt-0.5 shrink-0` so the content block reads as a single unit. _(effort: xs)_
- [ ] **[/frameworks · mobile]** (Background treatment) The soft radial background texture adds atmosphere but slightly reduces perceived crispness around the content edges on a small screen. → Tone down the background effect on mobile by reducing opacity or removing one gradient layer under `sm`; e.g. `bg-none sm:bg-[radial-gradient(...)]` or lower blur/alpha values. _(effort: s)_
- [ ] **[/compare · mobile]** (Top navigation) The header controls occupy substantial horizontal space and crowd the first screen without clearly prioritizing the compare task. → Compress mobile nav by reducing control height to `h-11`, spacing to `gap-2`, and language switcher padding to `px-3`; optionally collapse non-active languages into a globe menu on mobile. _(effort: m)_
- [ ] **[/compare · mobile]** (Color system) The muted beige background and light borders are aesthetically pleasing but some low-contrast neutrals may fall short for non-text UI distinction on mobile. → Strengthen neutral tokens to at least `border-slate-300` for pills and `text-slate-600/700` for secondary copy; verify non-text contrast meets WCAG 1.4.11 with a minimum 3:1 against adjacent surfaces. _(effort: xs)_
- [ ] **[/benchmarks · mobile]** (Hero spacing) There is too much vertical spacing between the page title, info box, filter card, and results card, making the screen feel long and fragmented. → Standardize section rhythm to `space-y-6` on the page container and reduce card-to-card spacing from approximately 32px to 24px using `mt-6` instead of `mt-8`. _(effort: s)_
- [ ] **[/benchmarks · mobile]** (Color system) The beige page background and muted gray text create a slightly washed-out appearance that weakens contrast and product confidence. → Use a cleaner neutral surface stack such as `bg-neutral-50` for the page, `bg-white` for cards, and maintain body copy at `text-neutral-700` or darker to preserve WCAG AA contrast. _(effort: m)_
- [ ] **[/benchmarks · mobile]** (Timestamp metadata) The 'Zuletzt berechnet' line is visually close to the title but not clearly differentiated as metadata. → Style it as secondary meta text with `text-sm text-neutral-500 mt-1` and consider adding `tracking-normal` to avoid competing with the title. _(effort: xs)_
- [ ] **[/benchmarks · mobile]** (Interactive states) There is no visible evidence of focus or active states for inputs and buttons, which weakens accessibility and polish. → Add accessible focus styles across controls with `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-violet-500 focus-visible:ring-offset-2` and hover/pressed states for buttons and select triggers. _(effort: s)_
