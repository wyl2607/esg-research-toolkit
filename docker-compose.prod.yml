services:
  api:
    build:
      context: .
    ports:
      - "8001:8000"
    env_file:
      - .env.prod
    environment:
      APP_ENV: production
      APP_HOST: 0.0.0.0
      APP_PORT: 8000
      DATABASE_URL: sqlite:///./data/esg_toolkit.db
    volumes:
      - /opt/esg-data:/app/data
      - /opt/esg-reports:/app/reports
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
